
try
     requestHandling
catch
     responseHandler(error)


Response ______________

server 찾기(보류)
     - ip,port,server_name(Host)
     - redirection, 3xx, redirect명령어로 response로 보냄.
     : server


location 찾기 함수 findLocation(uri) : 반환값 ConfigLocation
redirection찾고 있으면 , 3xx, redirect명령어로 response로 보냄.

Location관련 함수
     - limit exception.(method 되는지 안되는지 체크)
     - alias, auto_index, index.
     - 최종경로를 확인한다.
     : 반환 response

- 파일/디렉토리에 대한 접근 권한 확인(access 함수)

- index확인(디렉토리 인 경우) 
     - index.html이 있는지 확인(access) 
     - 없는데, autoindex on이면 파일 리스트를 만들어 준다.(in path, out body(page, string type))
     - 다 아니면 404 에러
     : response

- cgi 인지 아닌지(파일 인 경우) : 최종경로로 확인
     - cgi 아닌 경우(GET만)
          - 파일 열기
          - 에러(405)
     - cgi인 경우
          - pipe
          - event처리
          - fork처리
          - 자식프로세스 실행

response

- 자식 프로세의 파이프 읽기
     - 읽은 내용 파싱하기

Server    - Client(request, response, string)




epoll_wait
전체 event fd ./././././././.

epoll create
ep = epoll instance
epoll_add(ep, fd, evnet);
      fd1 - socket
      fd2 - write
      fd3 - d d
      fd4 - cgi pipe          read, write epoll
      read
epoll_wait(ep);
fds

=======================
외부 서버 읽는거 함수를







epoll
     listen
     add
